Addition:
- Panel Config:
    id: 200
  metric_table:
    id: 201
    metrics:
    - MFMA FLOPs (F6F4):
        value: SUM(SQ_INSTS_VALU_MFMA_MOPS_F6F4 * 512) / SUM(End_Timestamp - Start_Timestamp)
        unit: GFLOP/s
        peak: ((($max_sclk * $cu_per_gpu) * 16834) / 1000)
        pop: 100 * SUM(SQ_INSTS_VALU_MFMA_MOPS_F6F4 * 512) / SUM(End_Timestamp - Start_Timestamp) / ($max_sclk * $cu_per_gpu * 16834 / 1000)
- Panel Config:
    id: 300
  metric_table:
    id: 301
    metrics:
    - VL1 Lat:
        value: ROUND(SUM(TCP_TCP_LATENCY_sum) / SUM(TCP_TA_TCP_STATE_READ_sum), 0)
    - L2 Rd Lat:
        value: ROUND(SUM(TCP_TCC_READ_REQ_LATENCY_sum) / SUM(TCP_TCC_READ_REQ_sum + TCP_TCC_ATOMIC_WITH_RET_REQ_sum), 0)
    - L2 Wr Lat:
        value: ROUND(SUM(TCP_TCC_WRITE_REQ_LATENCY_sum) / SUM(TCP_TCC_WRITE_REQ_sum + TCP_TCC_ATOMIC_WITHOUT_RET_REQ_sum), 0)
- Panel Config:
    id: 300
  metric_descriptions:
    VL1 Lat: Calculated as the average number of cycles that a vL1D cache line request spent in the vL1D cache pipeline.
    L2 Rd Lat: Calculated as the average number of cycles that the vL1D cache took to issue and receive read requests from the L2 Cache. This number also includes requests for atomics with return values.
    L2 Wr Lat: Calculated as the average number of cycles that the vL1D cache took to issue and receive acknowledgement of a write request to the L2 Cache. This number also includes requests for atomics without return values.
- Panel Config:
    id: 400
  metric_table:
    id: 401
    metrics:
    - MFMA FLOPs (F6F4):
        value: SUM(SQ_INSTS_VALU_MFMA_MOPS_F6F4 * 512) / SUM(End_Timestamp - Start_Timestamp)
        unit: GFLOP/s
        peak: $MFMAF6F4Flops_empirical_peak
- Panel Config:
    id: 400
  metric_descriptions:
    MFMA FLOPs (F6F4): >-
      The total number of 4-bit and 6-bit floating point MFMA operations executed
      per second. Note: this does not include any floating point operations from
      VALU instructions. The peak empirically measured F6F4 MFMA operations
      achievable on the specific accelerator is displayed alongside for comparison.
      It is supported on AMD Instinct MI350 series (gfx950) and later only.
- Panel Config:
    id: 500
  metric_table:
    id: 502
    metrics:
    - CPC SYNC FIFO Full Rate:
        avg: SUM(100 * CPC_SYNC_FIFO_FULL) / SUM(CPC_SYNC_WRREQ_FIFO_BUSY)
        min: MIN(100 * CPC_SYNC_FIFO_FULL / CPC_SYNC_WRREQ_FIFO_BUSY)
        max: MAX(100 * CPC_SYNC_FIFO_FULL / CPC_SYNC_WRREQ_FIFO_BUSY)
        unit: Percent
    - CPC CANE Stall Rate:
        avg: SUM(100 * CPC_CANE_STALL) / SUM(CPC_CANE_BUSY)
        min: MIN(100 * CPC_CANE_STALL / CPC_CANE_BUSY)
        max: MAX(100 * CPC_CANE_STALL / CPC_CANE_BUSY)
        unit: Percent
    - CPC ADC Utilization:
        avg: SUM(100 * CPC_TG_SEND) / SUM(CPC_GD_BUSY)
        min: MIN(100 * CPC_TG_SEND / CPC_GD_BUSY)
        max: MAX(100 * CPC_TG_SEND / CPC_GD_BUSY)
        unit: Percent
- Panel Config:
    id: 500
  metric_descriptions:
    CPC SYNC FIFO Full Rate: Percent of CPC sync counter request FIFO busy cycles where the FIFO was full. High values indicate backpressure in synchronization counter processing.
    CPC CANE Stall Rate: Percent of CPC CANE bus busy cycles where sync counter requests were stalled. High values indicate contention on the CANE bus for synchronization counter operations.
    CPC ADC Utilization: Percent of ADC busy cycles spent dispatching thread groups. The ADC is responsible for sending workgroups from the command processor to the workgroup manager.
- Panel Config:
    id: 600
  metric_table:
    id: 601
    metrics:
    - Schedule-Pipe Wave Occupancy:
        avg: AVG(SPI_CSQ_P0_OCCUPANCY + SPI_CSQ_P1_OCCUPANCY + SPI_CSQ_P2_OCCUPANCY + SPI_CSQ_P3_OCCUPANCY)
        min: MIN(SPI_CSQ_P0_OCCUPANCY + SPI_CSQ_P1_OCCUPANCY + SPI_CSQ_P2_OCCUPANCY + SPI_CSQ_P3_OCCUPANCY)
        max: MAX(SPI_CSQ_P0_OCCUPANCY + SPI_CSQ_P1_OCCUPANCY + SPI_CSQ_P2_OCCUPANCY + SPI_CSQ_P3_OCCUPANCY)
        unit: Wave
    - Scheduler-Pipe Wave Utilization:
        avg: SUM(100 * SPI_CSC_WAVE_CNT_BUSY) / SUM($GRBM_GUI_ACTIVE_PER_XCD * $pipes_per_gpu * $se_per_gpu)
        min: MIN(100 * (SPI_CSC_WAVE_CNT_BUSY) / ($GRBM_GUI_ACTIVE_PER_XCD * $pipes_per_gpu * $se_per_gpu))
        max: MAX(100 * (SPI_CSC_WAVE_CNT_BUSY) / ($GRBM_GUI_ACTIVE_PER_XCD * $pipes_per_gpu * $se_per_gpu))
        unit: Percent
- Panel Config:
    id: 600
  metric_table:
    id: 602
    metrics:
    - Scheduler-Pipe FIFO Full Rate:
        avg: SUM(100 * (SPI_CS0_CRAWLER_STALL + SPI_CS1_CRAWLER_STALL + SPI_CS2_CRAWLER_STALL + SPI_CS3_CRAWLER_STALL)) / SUM($GRBM_SPI_BUSY_PER_XCD * $se_per_gpu)
        min: MIN(100 * (SPI_CS0_CRAWLER_STALL + SPI_CS1_CRAWLER_STALL + SPI_CS2_CRAWLER_STALL + SPI_CS3_CRAWLER_STALL) / ($GRBM_SPI_BUSY_PER_XCD * $se_per_gpu))
        max: MAX(100 * (SPI_CS0_CRAWLER_STALL + SPI_CS1_CRAWLER_STALL + SPI_CS2_CRAWLER_STALL + SPI_CS3_CRAWLER_STALL) / ($GRBM_SPI_BUSY_PER_XCD * $se_per_gpu))
        unit: Percent
- Panel Config:
    id: 600
  metric_descriptions:
    Schedule-Pipe Wave Occupancy: Total number of waves occupying all scheduler-pipe queues. The SPI has 4 scheduler-pipes, each with 8 hardware queues.
    Scheduler-Pipe Wave Utilization: Percent of total scheduler-pipe cycles when waves are actively resident in the scheduler-pipe structures. This measures wave presence in the pipes before dispatch to compute units.
    Scheduler-Pipe FIFO Full Rate: Percent of workgroup manager busy cycles where the event/wave order FIFO was full. High values indicate backpressure in the scheduler-pipe dispatch ordering logic.
- Panel Config:
    id: 1000
  metric_table:
    id: 1003
    metrics:
    - Spill/Stack Coalesceable Instr:
        avg: SUM(TA_BUFFER_COALESCEABLE_WAVEFRONTS_sum) / SUM($denom)
        min: MIN((TA_BUFFER_COALESCEABLE_WAVEFRONTS_sum / $denom))
        max: MAX((TA_BUFFER_COALESCEABLE_WAVEFRONTS_sum / $denom))
        unit: (Instructions + $normUnit)
- Panel Config:
    id: 1000
  metric_table:
    id: 1004
    metrics:
    - MFMA-F6F4:
        avg: SUM(SQ_INSTS_VALU_MFMA_F6F4) / SUM($denom)
        min: MIN((SQ_INSTS_VALU_MFMA_F6F4 / $denom))
        max: MAX((SQ_INSTS_VALU_MFMA_F6F4 / $denom))
        unit: (Instructions + $normUnit)
- Panel Config:
    id: 1000
  metric_descriptions:
    Spill/Stack Coalesceable Instr: The total number of coalesceable spill/stack memory instructions executed on all compute units, per normalization unit. Higher values indicate better memory access patterns for private memory operations.
    MFMA-F6F4: The total number of 4-bit and 6-bit floating point MFMA instructions issued per normalization unit. This is supported in AMD Instinct MI350 series (gfx950) and later only.
- Panel Config:
    id: 1100
  metric_table:
    id: 1101
    metrics:
    - MFMA FLOPs (F6F4):
        value: SUM(SQ_INSTS_VALU_MFMA_MOPS_F6F4 * 512) / SUM(End_Timestamp - Start_Timestamp)
        unit: GFLOPs
        peak: ((($max_sclk * $cu_per_gpu) * 16834) / 1000)
        pop: 100 * SUM(SQ_INSTS_VALU_MFMA_MOPS_F6F4 * 512) / SUM(End_Timestamp - Start_Timestamp) / ($max_sclk * $cu_per_gpu * 16834 / 1000)
- Panel Config:
    id: 1100
  metric_table:
    id: 1102
    metrics:
    - Dual-issue VALU Utilization:
        avg: SUM(100 * SQ_ACTIVE_INST_VALU2) / SUM($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu)
        min: MIN((((100 * SQ_ACTIVE_INST_VALU2) / $GRBM_GUI_ACTIVE_PER_XCD) / $cu_per_gpu))
        max: MAX((((100 * SQ_ACTIVE_INST_VALU2) / $GRBM_GUI_ACTIVE_PER_XCD) / $cu_per_gpu))
        unit: Percent
    - VALU Co-Issue Efficiency:
        avg: SUM(100 * SQ_ACTIVE_INST_VALU2) / SUM(SQ_ACTIVE_INST_VALU - SQ_ACTIVE_INST_VALU2)
        min: MIN((100 * SQ_ACTIVE_INST_VALU2) / (SQ_ACTIVE_INST_VALU - SQ_ACTIVE_INST_VALU2))
        max: MAX((100 * SQ_ACTIVE_INST_VALU2) / (SQ_ACTIVE_INST_VALU - SQ_ACTIVE_INST_VALU2))
        unit: Percent
- Panel Config:
    id: 1100
  metric_table:
    id: 1103
    metrics:
    - F6F4 OPs:
        avg: SUM(512 * SQ_INSTS_VALU_MFMA_MOPS_F6F4) / SUM($denom)
        min: MIN((512 * SQ_INSTS_VALU_MFMA_MOPS_F6F4) / $denom)
        max: MAX((512 * SQ_INSTS_VALU_MFMA_MOPS_F6F4) / $denom)
        unit: (FLOPs + $normUnit)
- Panel Config:
    id: 1100
  metric_descriptions:
    MFMA FLOPs (F8): >-
      The total number of 8-bit floating-point operations executed on MFMA units,
      per normalization unit. F8 (FP8) uses either E4M3 or E5M2 format.
    MFMA FLOPs (F6F4): >-
      The total number of 4-bit and 6-bit floating-point operations executed on
      MFMA units, per second. F6/F4 formats are supported in AMD Instinct
      MI350 series (gfx950) and later only.
    Dual-issue VALU Utilization: >-
      Indicates what percent of the kernel's duration the VALU was busy executing
      dual-issued instructions. Computed as the ratio of the total number of cycles
      spent by the scheduler co-issuing VALU instructions over the total
      CU cycles.
    VALU Co-Issue Efficiency: >-
      The ratio of quad-cycles where two VALU instructions were co-issued (executed
      simultaneously) to quad-cycles where only a single VALU instruction was issued.
      Measures how efficiently the kernel leverages the MI350's ability to co-issue
      VALU instructions. Unlike Dual-issue VALU Utilization which measures percentage
      of total time, this metric can exceed 100% when dual-issue cycles outnumber
      single-issue cycles.
    F8 OPs: >-
      The total number of 8-bit floating-point operations executed on MFMA units,
      per normalization unit. F8 (FP8) uses either E4M3 or E5M2 format.
    F6F4 OPs: >-
      The total number of 4-bit and 6-bit floating-point operations executed on
      MFMA units, per normalization unit. F6/F4 formats are supported in AMD Instinct
      MI350 series (gfx950) and later only.
- Panel Config:
    id: 1200
  metric_table:
    id: 1202
    metrics:
    - LDS LOAD:
        avg: SUM(SQ_INSTS_LDS_LOAD) / SUM($denom)
        min: MIN((SQ_INSTS_LDS_LOAD / $denom))
        max: MAX((SQ_INSTS_LDS_LOAD / $denom))
        unit: (Instructions + $normUnit)
    - LDS STORE:
        avg: SUM(SQ_INSTS_LDS_STORE) / SUM($denom)
        min: MIN((SQ_INSTS_LDS_STORE / $denom))
        max: MAX((SQ_INSTS_LDS_STORE / $denom))
        unit: (Instructions + $normUnit)
    - LDS ATOMIC:
        avg: SUM(SQ_INSTS_LDS_ATOMIC) / SUM($denom)
        min: MIN((SQ_INSTS_LDS_ATOMIC / $denom))
        max: MAX((SQ_INSTS_LDS_ATOMIC / $denom))
        unit: (Instructions + $normUnit)
    - LDS LOAD Bandwidth:
        avg: SUM(64 * SQ_INSTS_LDS_LOAD_BANDWIDTH) / SUM(End_Timestamp - Start_Timestamp)
        min: MIN(64 * SQ_INSTS_LDS_LOAD_BANDWIDTH / (End_Timestamp - Start_Timestamp))
        max: MAX(64 * SQ_INSTS_LDS_LOAD_BANDWIDTH / (End_Timestamp - Start_Timestamp))
        units: Gbps
    - LDS STORE Bandwidth:
        avg: SUM(64 * SQ_INSTS_LDS_STORE_BANDWIDTH) / SUM(End_Timestamp - Start_Timestamp)
        min: MIN(64 * SQ_INSTS_LDS_STORE_BANDWIDTH / (End_Timestamp - Start_Timestamp))
        max: MAX(64 * SQ_INSTS_LDS_STORE_BANDWIDTH / (End_Timestamp - Start_Timestamp))
        units: Gbps
    - LDS ATOMIC Bandwidth:
        avg: SUM(64 * SQ_INSTS_LDS_ATOMIC_BANDWIDTH) / SUM(End_Timestamp - Start_Timestamp)
        min: MIN(64 * SQ_INSTS_LDS_ATOMIC_BANDWIDTH / (End_Timestamp - Start_Timestamp))
        max: MAX(64 * SQ_INSTS_LDS_ATOMIC_BANDWIDTH / (End_Timestamp - Start_Timestamp))
        units: Gbps
    - LDS Command FIFO Full Rate:
        avg: SUM(SQ_LDS_CMD_FIFO_FULL) / SUM($denom)
        min: MIN((SQ_LDS_CMD_FIFO_FULL / $denom))
        max: MAX((SQ_LDS_CMD_FIFO_FULL / $denom))
        unit: (Cycles + $normUnit)
    - LDS Data FIFO Full Rate:
        avg: SUM(SQ_LDS_DATA_FIFO_FULL) / SUM($denom)
        min: MIN((SQ_LDS_DATA_FIFO_FULL / $denom))
        max: MAX((SQ_LDS_DATA_FIFO_FULL / $denom))
        unit: (Cycles + $normUnit)
- Panel Config:
    id: 1200
  metric_descriptions:
    LDS LOAD Bandwidth: >-
      The effective bandwidth of LDS load operations, accounting for the work-items
      executed (execution mask). Calculated as the total bytes loaded from LDS
      divided by the kernel duration.
    LDS STORE Bandwidth: >-
      The effective bandwidth of LDS store operations, accounting for the work-items
      executed (execution mask). Calculated as the total bytes stored to LDS
      divided by the kernel duration.
    LDS ATOMIC Bandwidth: >-
      The effective bandwidth of LDS atomic operations, accounting for the work-items
      executed (execution mask). Calculated as the total bytes accessed by LDS
      atomic operations divided by the kernel duration.
    LDS LOAD: >-
      The total number of LDS load instructions issued per normalization unit.
    LDS STORE: >-
      The total number of LDS store instructions issued per normalization unit.
    LDS ATOMIC: >-
      The total number of LDS atomic instructions issued per normalization unit.
    LDS Command FIFO Full Rate: >-
      The number of cycles where the LDS command FIFO was full, per normalization
      unit. High values indicate backpressure in LDS instruction dispatch, which
      may stall wavefronts waiting to issue LDS operations.
    LDS Data FIFO Full Rate: >-
      The number of cycles where the LDS data FIFO was full, per normalization
      unit. High values indicate backpressure in LDS data return path, which may
      stall wavefronts waiting for LDS read/atomic results.
- Panel Config:
    id: 1500
  metric_table:
    id: 1502
    metrics:
    - Global/Generic Read Instructions for LDS:
        avg: SUM(TA_FLAT_READ_LDS_WAVEFRONTS_sum) / SUM($denom)
        min: MIN((TA_FLAT_READ_LDS_WAVEFRONTS_sum / $denom))
        max: MAX((TA_FLAT_READ_LDS_WAVEFRONTS_sum / $denom))
        unit: (Instructions + $normUnit)
    - Spill/Stack Read Instructions for LDS:
        avg: SUM(TA_BUFFER_READ_LDS_WAVEFRONTS_sum) / SUM($denom)
        min: MIN((TA_BUFFER_READ_LDS_WAVEFRONTS_sum / $denom))
        max: MAX((TA_BUFFER_READ_LDS_WAVEFRONTS_sum / $denom))
        unit: (Instructions + $normUnit)
- Panel Config:
    id: 1500
  metric_table:
    id: 1504
    metrics:
    - Write Ack Instructions:
        avg: SUM(TD_WRITE_ACKT_WAVEFRONT_sum) / SUM($denom)
        min: MIN((TD_WRITE_ACKT_WAVEFRONT_sum / $denom))
        max: MAX((TD_WRITE_ACKT_WAVEFRONT_sum / $denom))
        unit: (Instructions + $normUnit)
- Panel Config:
    id: 1500
  metric_descriptions:
    Global/Generic Read Instructions for LDS: >-
      The total number of global & generic memory read instructions that return
      data directly to LDS, executed on all compute units on the accelerator, per
      normalization unit. These operations bypass the register file and write
      results directly into LDS memory.
    Spill/Stack Read Instructions for LDS: >-
      The total number of spill/stack memory read instructions that return data
      directly to LDS, executed on all compute units on the accelerator, per
      normalization unit. These operations bypass the register file and write
      results directly into LDS memory.
    Write Ack Instructions: The total number of write acknowledgements submitted by data-return unit to SQ, summed over all compute units on the accelerator, per normalization unit.
- Panel Config:
    id: 1600
  metric_table:
    id: 1602
    metrics:
    - Stalled on Address:
        expr: 100 * TCP_TCP_TA_ADDR_STALL_CYCLES_sum / TCP_GATE_EN1_sum
    - Stalled on Data:
        expr: 100 * TCP_TCP_TA_DATA_STALL_CYCLES_sum / TCP_GATE_EN1_sum
    - Stalled on Latency FIFO:
        expr: 100 * TCP_LFIFO_STALL_CYCLES_sum / TCP_GATE_EN1_sum
    - Stalled on Request FIFO:
        expr: 100 * TCP_RFIFO_STALL_CYCLES_sum / TCP_GATE_EN1_sum
    - Stalled on Read Return:
        expr: 100 * TCP_TCR_RDRET_STALL_sum / TCP_GATE_EN1_sum
- Panel Config:
    id: 1600
  metric_table:
    id: 1603
    metrics:
    - Tag RAM 0 Req:
        avg: SUM(TCP_TAGRAM0_REQ_sum) / SUM($denom)
        min: MIN((TCP_TAGRAM0_REQ_sum / $denom))
        max: MAX((TCP_TAGRAM0_REQ_sum / $denom))
        unit: (Requests + $normUnit)
    - Tag RAM 1 Req:
        avg: SUM(TCP_TAGRAM1_REQ_sum) / SUM($denom)
        min: MIN((TCP_TAGRAM1_REQ_sum / $denom))
        max: MAX((TCP_TAGRAM1_REQ_sum / $denom))
        unit: (Requests + $normUnit)
    - Tag RAM 2 Req:
        avg: SUM(TCP_TAGRAM2_REQ_sum) / SUM($denom)
        min: MIN((TCP_TAGRAM2_REQ_sum / $denom))
        max: MAX((TCP_TAGRAM2_REQ_sum / $denom))
        unit: (Requests + $normUnit)
    - Tag RAM 3 Req:
        avg: SUM(TCP_TAGRAM3_REQ_sum) / SUM($denom)
        min: MIN((TCP_TAGRAM3_REQ_sum / $denom))
        max: MAX((TCP_TAGRAM3_REQ_sum / $denom))
        unit: (Requests + $normUnit)
    - L1 Access Latency:
        avg: SUM(TCP_TCP_LATENCY_sum) / SUM($denom)
        min: MIN((TCP_TCP_LATENCY_sum / $denom))
        max: MAX((TCP_TCP_LATENCY_sum / $denom))
        unit: (Cycles + $normUnit)
    - L1-L2 Read Latency:
        avg: SUM(TCP_TCC_READ_REQ_LATENCY_sum) / SUM($denom)
        min: MIN((TCP_TCC_READ_REQ_LATENCY_sum / $denom))
        max: MAX((TCP_TCC_READ_REQ_LATENCY_sum / $denom))
        unit: (Cycles + $normUnit)
    - L1-L2 Write Latency:
        avg: SUM(TCP_TCC_WRITE_REQ_LATENCY_sum) / SUM($denom)
        min: MIN((TCP_TCC_WRITE_REQ_LATENCY_sum / $denom))
        max: MAX((TCP_TCC_WRITE_REQ_LATENCY_sum / $denom))
        unit: (Cycles + $normUnit)
- Panel Config:
    id: 1600
  metric_table:
    id: 1605
    metrics:
    - Inflight Req:
        avg: SUM(TCP_CLIENT_UTCL1_INFLIGHT_sum) / SUM($denom)
        min: MIN((TCP_CLIENT_UTCL1_INFLIGHT_sum / $denom))
        max: MAX((TCP_CLIENT_UTCL1_INFLIGHT_sum / $denom))
        units: (Requests + $normUnit)
    - Misses under Translation Miss:
        avg: SUM(TCP_UTCL1_TRANSLATION_MISS_UNDER_MISS_sum) / SUM($denom)
        min: MIN((TCP_UTCL1_TRANSLATION_MISS_UNDER_MISS_sum / $denom))
        max: MAX((TCP_UTCL1_TRANSLATION_MISS_UNDER_MISS_sum / $denom))
        units: (Requests + $normUnit)
- Panel Config:
    id: 1600
  metric_table:
    id: 1606
    metrics:
    - Cache Full Stall:
        avg: SUM(TCP_UTCL1_STALL_INFLIGHT_MAX_sum) / SUM($denom)
        min: MIN((TCP_UTCL1_STALL_INFLIGHT_MAX_sum / $denom))
        max: MAX((TCP_UTCL1_STALL_INFLIGHT_MAX_sum / $denom))
        units: (Cycles + $normUnit)
    - Cache Miss Stall:
        avg: SUM(TCP_UTCL1_STALL_MULTI_MISS_sum) / SUM($denom)
        min: MIN((TCP_UTCL1_STALL_MULTI_MISS_sum / $denom))
        max: MAX((TCP_UTCL1_STALL_MULTI_MISS_sum / $denom))
        units: (Cycles + $normUnit)
    - Serialization Stall:
        avg: SUM(TCP_UTCL1_SERIALIZATION_STALL_sum) / SUM($denom)
        min: MIN((TCP_UTCL1_SERIALIZATION_STALL_sum / $denom))
        max: MAX((TCP_UTCL1_SERIALIZATION_STALL_sum / $denom))
        units: (Cycles + $normUnit)
    - Thrashing Stall:
        avg: SUM(TCP_UTCL1_THRASHING_STALL_sum) / SUM($denom)
        min: MIN((TCP_UTCL1_THRASHING_STALL_sum / $denom))
        max: MAX((TCP_UTCL1_THRASHING_STALL_sum / $denom))
        units: (Cycles + $normUnit)
    - Latency FIFO Stall:
        avg: SUM(TCP_UTCL1_LFIFO_FULL_sum) / SUM($denom)
        min: MIN((TCP_UTCL1_LFIFO_FULL_sum / $denom))
        max: MAX((TCP_UTCL1_LFIFO_FULL_sum / $denom))
        units: (Cycles + $normUnit)
    - Resident Page Full Stall:
        avg: SUM(TCP_UTCL1_STALL_LFIFO_NO_RES_sum) / SUM($denom)
        min: MIN((TCP_UTCL1_STALL_LFIFO_NO_RES_sum / $denom))
        max: MAX((TCP_UTCL1_STALL_LFIFO_NO_RES_sum / $denom))
        units: (Cycles + $normUnit)
    - UTCL2 Stall:
        avg: SUM(TCP_UTCL1_STALL_UTCL2_REQ_OUT_OF_CREDITS_sum) / SUM($denom)
        min: MIN((TCP_UTCL1_STALL_UTCL2_REQ_OUT_OF_CREDITS_sum / $denom))
        max: MAX((TCP_UTCL1_STALL_UTCL2_REQ_OUT_OF_CREDITS_sum / $denom))
        units: (Cycles + $normUnit)
- Panel Config:
    id: 1600
  metric_descriptions:
    Stalled on Address: >-
      The ratio of the number of cycles where the vL1D is stalled waiting for the
      address processing unit to send address information, divided by the number
      of cycles where the vL1D is active.
    Stalled on Data: >-
      The ratio of the number of cycles where the vL1D is stalled waiting for the
      address processing unit to send write/atomic data, divided by the number of
      cycles where the vL1D is active.
    Stalled on Latency FIFO: >-
      The ratio of the number of cycles where the vL1D is stalled because the
      latency FIFO (tracking outstanding requests) is full, divided by the number
      of cycles where the vL1D is active.
    Stalled on Request FIFO: >-
      The ratio of the number of cycles where the vL1D is stalled because the
      request FIFO (queuing memory requests) is full, divided by the number of
      cycles where the vL1D is active.
    Stalled on Read Return: >-
      The ratio of the number of cycles where the vL1D is stalled waiting for read
      data to return from L2 before it can write into the cache, divided by the
      number of cycles where the vL1D is active.
    Tag RAM 0 Req: >-
      The total number of vL1D cache requests (cache line lookups) that mapped to
      Tag RAM bank 0 during the vL1D tag lookup process, per normalization unit.
      The vL1D cache uses multiple Tag RAM banks for parallel tag lookups.
      Distribution across banks affects lookup efficiency.
    Tag RAM 1 Req: >-
      The total number of vL1D cache requests (cache line lookups) that mapped to
      Tag RAM bank 1 during the vL1D tag lookup process, per normalization unit.
      The vL1D cache uses multiple Tag RAM banks for parallel tag lookups.
      Distribution across banks affects lookup efficiency.
    Tag RAM 2 Req: >-
      The total number of vL1D cache requests (cache line lookups) that mapped to
      Tag RAM bank 2 during the vL1D tag lookup process, per normalization unit.
      The vL1D cache uses multiple Tag RAM banks for parallel tag lookups.
      Distribution across banks affects lookup efficiency.
    Tag RAM 3 Req: >-
      The total number of vL1D cache requests (cache line lookups) that mapped to
      Tag RAM bank 3 during the vL1D tag lookup process, per normalization unit.
      The vL1D cache uses multiple Tag RAM banks for parallel tag lookups.
      Distribution across banks affects lookup efficiency.
    L1 Access Latency: Calculated as the average number of cycles that a vL1D cache line request spent in the vL1D cache pipeline.
    L1-L2 Read Latency: Calculated as the average number of cycles that the vL1D cache took to issue and receive read requests from the L2 Cache. This number also includes requests for atomics with return values.
    L1-L2 Write Latency: Calculated as the average number of cycles that the vL1D cache took to issue and receive acknowledgement of a write request to the L2 Cache. This number also includes requests for atomics without return values.
    Inflight Req: >-
      The sum of inflight client-to-UTCL1 translation requests per cycle, per
      normalization unit. Measures the average number of translation requests
      actively being processed by the UTCL1 at any given time.
    Misses under Translation Miss: >-
      The number of translation requests that missed in the UTCL1 while another
      miss for the same translation was already being processed, per normalization
      unit. These are secondary misses that occur before the first miss completes.
    Cache Full Stall: >-
      The number of cycles the UTCL1 was stalled due to the inflight request
      counter reaching its maximum capacity, per normalization unit. Indicates
      the translation cache cannot accept more requests.
    Cache Miss Stall: >-
      The number of cycles the UTCL1 was stalled due to multiple cache misses
      being arbitrated simultaneously, per normalization unit.
    Serialization Stall: >-
      The number of cycles the UTCL1 was stalled due to serializing translation
      requests through the cache, per normalization unit.
    Thrashing Stall: >-
      The number of cycles the UTCL1 was stalled due to cache thrashing (rapid
      eviction and reload of translations), per normalization unit. This is an
      estimation when thrashing detection is active.
    Latency FIFO Stall: >-
      The number of cycles the UTCL1-to-UTCL2 latency-hiding FIFO was full,
      per normalization unit. This FIFO queues requests to the L2 translation
      cache (UTCL2).
    Resident Page Full Stall: >-
      The number of cycles the UTCL1 was stalled because the latency-hiding
      FIFO output indicated a non-resident page, per normalization unit.
    UTCL2 Stall: >-
      The number of cycles the UTCL1 was stalled due to running out of request
      credits for the UTCL2 (L2 translation cache), per normalization unit.
- Panel Config:
    id: 1700
  metric_table:
    id: 1702
    metrics:
    - Read Stall:
        avg: SUM(100 * (TCC_EA0_RDREQ_IO_CREDIT_STALL_sum + TCC_EA0_RDREQ_GMI_CREDIT_STALL_sum + TCC_EA0_RDREQ_DRAM_CREDIT_STALL_sum)) / SUM(TCC_BUSY_sum)
        min: MIN(100 * (TCC_EA0_RDREQ_IO_CREDIT_STALL_sum + TCC_EA0_RDREQ_GMI_CREDIT_STALL_sum + TCC_EA0_RDREQ_DRAM_CREDIT_STALL_sum) / TCC_BUSY_sum)
        max: MAX(100 * (TCC_EA0_RDREQ_IO_CREDIT_STALL_sum + TCC_EA0_RDREQ_GMI_CREDIT_STALL_sum + TCC_EA0_RDREQ_DRAM_CREDIT_STALL_sum) / TCC_BUSY_sum)
        unit: Percent
    - Write Stall:
        avg: SUM(100 * TCC_EA0_WRREQ_STALL_sum) / SUM(TCC_BUSY_sum)
        min: MIN(100 * TCC_EA0_WRREQ_STALL_sum / TCC_BUSY_sum)
        max: MAX(100 * TCC_EA0_WRREQ_STALL_sum / TCC_BUSY_sum)
        unit: Percent
- Panel Config:
    id: 1700
  metric_table:
    id: 1703
    metrics:
    - Read Bandwidth:
        avg: SUM(TCC_READ_SECTORS_sum * 32) / SUM(End_Timestamp - Start_Timestamp)
        min: MIN(TCC_READ_SECTORS_sum * 32/ (End_Timestamp - Start_Timestamp))
        max: MAX(TCC_READ_SECTORS_sum * 32/ (End_Timestamp - Start_Timestamp))
        unit: Gbps
    - Write Bandwidth:
        avg: SUM(TCC_WRITE_SECTORS_sum * 32) / SUM(End_Timestamp - Start_Timestamp)
        min: MIN(TCC_WRITE_SECTORS_sum * 32/ (End_Timestamp - Start_Timestamp))
        max: MAX(TCC_WRITE_SECTORS_sum * 32/ (End_Timestamp - Start_Timestamp))
        unit: Gbps
    - Atomic Bandwidth:
        avg: SUM(TCC_ATOMIC_SECTORS_sum * 32) / SUM(End_Timestamp - Start_Timestamp)
        min: MIN(TCC_ATOMIC_SECTORS_sum * 32/ (End_Timestamp - Start_Timestamp))
        max: MAX(TCC_ATOMIC_SECTORS_sum * 32/ (End_Timestamp - Start_Timestamp))
        unit: Gbps
    - Bypass Req:
        avg: SUM(TCC_BYPASS_REQ_sum) / SUM($denom)
        min: MIN((TCC_BYPASS_REQ_sum / $denom))
        max: MAX((TCC_BYPASS_REQ_sum / $denom))
        unit: (Req + $normUnit)
    - Input Buffer Req:
        avg: SUM(TCC_IB_REQ_sum) / SUM($denom)
        min: MIN((TCC_IB_REQ_sum / $denom))
        max: MAX((TCC_IB_REQ_sum / $denom))
        unit: (Req + $normUnit)
- Panel Config:
    id: 1700
  metric_table:
    id: 1704
    metrics:
    - Stalled on Latency FIFO:
        avg: SUM(TCC_LATENCY_FIFO_FULL_sum) / SUM($denom)
        min: MIN(TCC_LATENCY_FIFO_FULL_sum / $denom)
        max: MAX(TCC_LATENCY_FIFO_FULL_sum / $denom)
        unit: (Cycles + $normUnit)
    - Stalled on Write Data FIFO:
        avg: SUM(TCC_SRC_FIFO_FULL_sum) / SUM($denom)
        min: MIN(TCC_SRC_FIFO_FULL_sum / $denom)
        max: MAX(TCC_SRC_FIFO_FULL_sum / $denom)
        unit: (Cycles + $normUnit)
    - Input Buffer Stalled on L2:
        avg: SUM(TCC_IB_STALL_sum) / SUM($denom)
        min: MIN(TCC_IB_STALL_sum / $denom)
        max: MAX(TCC_IB_STALL_sum / $denom)
        unit: (Cycles + $normUnit)
- Panel Config:
    id: 1700
  metric_table:
    id: 1705
    metrics:
    - Read - PCIe Stall:
        type: PCIe Stall
        transaction: Read
        avg: SUM(100 * TCC_EA0_RDREQ_IO_CREDIT_STALL_sum) / SUM(TCC_BUSY_sum)
        min: MIN(100 * TCC_EA0_RDREQ_IO_CREDIT_STALL_sum / TCC_BUSY_sum)
        max: MAX(100 * TCC_EA0_RDREQ_IO_CREDIT_STALL_sum / TCC_BUSY_sum)
        unit: Percent
    - Read - Infinity Fabric Stall:
        type: "Infinity Fabric™ Stall"
        transaction: Read
        avg: SUM(100 * TCC_EA0_RDREQ_GMI_CREDIT_STALL_sum) / SUM(TCC_BUSY_sum)
        min: MIN(100 * TCC_EA0_RDREQ_GMI_CREDIT_STALL_sum / TCC_BUSY_sum)
        max: MAX(100 * TCC_EA0_RDREQ_GMI_CREDIT_STALL_sum / TCC_BUSY_sum)
        unit: Percent
    - Read - HBM Stall:
        type: HBM Stall
        transaction: Read
        avg: SUM(100 * TCC_EA0_RDREQ_DRAM_CREDIT_STALL_sum) / SUM(TCC_BUSY_sum)
        min: MIN(100 * TCC_EA0_RDREQ_DRAM_CREDIT_STALL_sum / TCC_BUSY_sum)
        max: MAX(100 * TCC_EA0_RDREQ_DRAM_CREDIT_STALL_sum / TCC_BUSY_sum)
        unit: Percent
    - Write - PCIe Stall:
        type: PCIe Stall
        transaction: Write
        avg: SUM(100 * TCC_EA0_WRREQ_IO_CREDIT_STALL_sum) / SUM(TCC_BUSY_sum)
        min: MIN(100 * TCC_EA0_WRREQ_IO_CREDIT_STALL_sum / TCC_BUSY_sum)
        max: MAX(100 * TCC_EA0_WRREQ_IO_CREDIT_STALL_sum / TCC_BUSY_sum)
        unit: Percent
    - Write - Infinity Fabric Stall:
        type: "Infinity Fabric™ Stall"
        transaction: Write
        avg: SUM(100 * TCC_EA0_WRREQ_GMI_CREDIT_STALL_sum) / SUM(TCC_BUSY_sum)
        min: MIN(100 * TCC_EA0_WRREQ_GMI_CREDIT_STALL_sum / TCC_BUSY_sum)
        max: MAX(100 * TCC_EA0_WRREQ_GMI_CREDIT_STALL_sum / TCC_BUSY_sum)
        unit: Percent
    - Write - HBM Stall:
        type: HBM Stall
        transaction: Write
        avg: SUM(100 * TCC_EA0_WRREQ_DRAM_CREDIT_STALL_sum) / SUM(TCC_BUSY_sum)
        min: MIN(100 * TCC_EA0_WRREQ_DRAM_CREDIT_STALL_sum / TCC_BUSY_sum)
        max: MAX(100 * TCC_EA0_WRREQ_DRAM_CREDIT_STALL_sum / TCC_BUSY_sum)
        unit: Percent
- Panel Config:
    id: 1700
  metric_table:
    id: 1706
    metrics:
    - Read (128B):
        avg: SUM(TCC_EA0_RDREQ_128B_sum) / SUM($denom)
        min: MIN((TCC_EA0_RDREQ_128B_sum / $denom))
        max: MAX((TCC_EA0_RDREQ_128B_sum / $denom))
        unit: (Req + $normUnit)
    - Read Bandwidth - PCIe:
        avg: SUM(TCC_EA0_RDREQ_IO_32B_sum * 32) / SUM(End_Timestamp - Start_Timestamp)
        min: MIN(TCC_EA0_RDREQ_IO_32B_sum * 32/ (End_Timestamp - Start_Timestamp))
        max: MAX(TCC_EA0_RDREQ_IO_32B_sum * 32/ (End_Timestamp - Start_Timestamp))
        unit: Gbps
    - "Read Bandwidth - Infinity Fabric™":
        avg: SUM(TCC_EA0_RDREQ_GMI_32B_sum * 32) / SUM(End_Timestamp - Start_Timestamp)
        min: MIN(TCC_EA0_RDREQ_GMI_32B_sum * 32/ (End_Timestamp - Start_Timestamp))
        max: MAX(TCC_EA0_RDREQ_GMI_32B_sum * 32/ (End_Timestamp - Start_Timestamp))
        unit: Gbps
    - Read Bandwidth - HBM:
        avg: SUM(TCC_EA0_RDREQ_DRAM_32B_sum * 32) / SUM(End_Timestamp - Start_Timestamp)
        min: MIN(TCC_EA0_RDREQ_DRAM_32B_sum * 32/ (End_Timestamp - Start_Timestamp))
        max: MAX(TCC_EA0_RDREQ_DRAM_32B_sum * 32/ (End_Timestamp - Start_Timestamp))
        unit: Gbps
    - Write Bandwidth - PCIe:
        avg: SUM(TCC_EA0_WRREQ_WRITE_IO_32B_sum * 32) / SUM(End_Timestamp - Start_Timestamp)
        min: MIN(TCC_EA0_WRREQ_WRITE_IO_32B_sum * 32/ (End_Timestamp - Start_Timestamp))
        max: MAX(TCC_EA0_WRREQ_WRITE_IO_32B_sum * 32/ (End_Timestamp - Start_Timestamp))
        unit: Gbps
    - "Write Bandwidth - Infinity Fabric™":
        avg: SUM(TCC_EA0_WRREQ_WRITE_GMI_32B_sum * 32) / SUM(End_Timestamp - Start_Timestamp)
        min: MIN(TCC_EA0_WRREQ_WRITE_GMI_32B_sum * 32/ (End_Timestamp - Start_Timestamp))
        max: MAX(TCC_EA0_WRREQ_WRITE_GMI_32B_sum * 32/ (End_Timestamp - Start_Timestamp))
        unit: Gbps
    - Write Bandwidth - HBM:
        avg: SUM(TCC_EA0_WRREQ_WRITE_DRAM_32B_sum * 32) / SUM(End_Timestamp - Start_Timestamp)
        min: MIN(TCC_EA0_WRREQ_WRITE_DRAM_32B_sum * 32/ (End_Timestamp - Start_Timestamp))
        max: MAX(TCC_EA0_WRREQ_WRITE_DRAM_32B_sum * 32/ (End_Timestamp - Start_Timestamp))
        unit: Gbps
    - Atomic - HBM:
        avg: SUM(TCC_EA0_WRREQ_ATOMIC_DRAM_sum) / SUM($denom)
        min: MIN((TCC_EA0_WRREQ_ATOMIC_DRAM_sum / $denom))
        max: MAX((TCC_EA0_WRREQ_ATOMIC_DRAM_sum / $denom))
        unit: (Req + $normUnit)
    - Atomic Bandwidth - PCIe:
        avg: SUM(TCC_EA0_WRREQ_ATOMIC_IO_32B_sum * 32) / SUM(End_Timestamp - Start_Timestamp)
        min: MIN(TCC_EA0_WRREQ_ATOMIC_IO_32B_sum * 32/ (End_Timestamp - Start_Timestamp))
        max: MAX(TCC_EA0_WRREQ_ATOMIC_IO_32B_sum * 32/ (End_Timestamp - Start_Timestamp))
        unit: Gbps
    - "Atomic Bandwidth - Infinity Fabric™":
        avg: SUM(TCC_EA0_WRREQ_ATOMIC_GMI_32B_sum * 32) / SUM(End_Timestamp - Start_Timestamp)
        min: MIN(TCC_EA0_WRREQ_ATOMIC_GMI_32B_sum * 32/ (End_Timestamp - Start_Timestamp))
        max: MAX(TCC_EA0_WRREQ_ATOMIC_GMI_32B_sum * 32/ (End_Timestamp - Start_Timestamp))
        unit: Gbps
    - Atomic Bandwidth - HBM:
        avg: SUM(TCC_EA0_WRREQ_ATOMIC_DRAM_32B_sum * 32) / SUM(End_Timestamp - Start_Timestamp)
        min: MIN(TCC_EA0_WRREQ_ATOMIC_DRAM_32B_sum * 32/ (End_Timestamp - Start_Timestamp))
        max: MAX(TCC_EA0_WRREQ_ATOMIC_DRAM_32B_sum * 32/ (End_Timestamp - Start_Timestamp))
        unit: Gbps
- Panel Config:
    id: 1700
  metric_descriptions:
    Read Bandwidth: Total number of bytes looked up in the L2 cache for read requests, divided by total duration.
    Write Bandwidth: Total number of bytes looked up in the L2 cache for write requests, divided by total duration.
    Atomic Bandwidth: Total number of bytes looked up in the L2 cache for atomic requests, divided by total duration.
    Bypass Req: Number of L2 cache bypass requests measured at the tag block. These requests skip normal cache lookup and go directly to memory, typically for uncacheable or write-combined memory accesses.
    Input Buffer Req: Number of raw memory requests received through the L2 cache Input Buffer (IB) from compute units and other hardware clients. This counts all incoming requests before cache lookup processing and represents the total incoming demand on the L2 cache.
    Read (128B): The total number of L2 requests to Infinity Fabric to read 128B of data from any memory location, per normalization unit.
    Read Bandwidth - PCIe: Total number of bytes due to L2 read requests due to PCIe traffic, divided by total duration.
    "Read Bandwidth - Infinity Fabric™": Total number of bytes due to L2 read requests due to Infinity Fabric traffic, divided by total duration.
    Read Bandwidth - HBM: Total number of bytes due to L2 read requests due to HBM traffic, divided by total duration.
    Write Bandwidth - PCIe: Total number of bytes due to L2 write requests due to PCIe traffic, divided by total duration.
    "Write Bandwidth - Infinity Fabric™": Total number of bytes due to L2 write requests due to Infinity Fabric traffic, divided by total duration.
    Write Bandwidth - HBM: Total number of bytes due to L2 write requests due to HBM traffic, divided by total duration.
    Atomic Bandwidth - PCIe: Total number of bytes due to L2 atomic requests due to PCIe traffic, divided by total duration.
    "Atomic Bandwidth - Infinity Fabric™": Total number of bytes due to L2 atomic requests due to Infinity Fabric traffic, divided by total duration.
    Atomic Bandwidth - HBM: Total number of bytes due to L2 atomic requests due to HBM traffic, divided by total duration.
    Atomic - HBM: The total number of L2 atomic requests (either 32-byte or 64-byte) destined for the accelerator's local high-bandwidth memory (HBM), per normalization unit. These are read-modify-write operations that update memory atomically.
    Read Stall: >-
      The ratio of the total number of cycles the L2-Fabric interface was
      stalled on a read request to any destination (local HBM, remote PCIe\xAE
      connected accelerator or CPU, or remote Infinity Fabric connected accelerator
      or CPU) over the total active L2 cycles.
    Write Stall: The ratio of the total number of cycles the L2-Fabric interface was stalled on a write or atomic request to any destination (local HBM, remote accelerator or CPU, PCIe connected accelerator or CPU, or remote Infinity Fabric connected accelerator or CPU) over the total active L2 cycles.
    Read - PCIe Stall: The number of cycles the L2-Fabric interface was stalled on read requests to remote PCIe connected accelerators or CPUs as a percent of the total active L2 cycles.
    Read - Infinity Fabric Stall: The number of cycles the L2-Fabric interface was stalled on read requests to remote Infinity Fabric connected accelerators or CPUs as a percent of the total active L2 cycles.
    Read - HBM Stall: The number of cycles the L2-Fabric interface was stalled on read requests to the accelerator's local HBM as a percent of the total active L2 cycles.
    Write - PCIe Stall: The number of cycles the L2-Fabric interface was stalled on write or atomic requests to remote PCIe connected accelerators or CPUs as a percent of the total active L2 cycles.
    Write - Infinity Fabric Stall: The number of cycles the L2-Fabric interface was stalled on write or atomic requests to remote Infinity Fabric connected accelerators or CPUs as a percent of the total active L2 cycles.
    Write - HBM Stall: The number of cycles the L2-Fabric interface was stalled on write or atomic requests to accelerator's local HBM as a percent of the total active L2 cycles.
Modification:
- Panel Config:
    id: 200
  metric_table:
    id: 201
    metrics:
    - MFMA FLOPs (F8):
        unit: GFLOP/s
        peak: ((($max_sclk * $cu_per_gpu) * 8192) / 1000)
        pop: 100 * SUM(SQ_INSTS_VALU_MFMA_MOPS_F8 * 512) / SUM(End_Timestamp - Start_Timestamp) / ($max_sclk * $cu_per_gpu * 8192 / 1000)
    - MFMA FLOPs (BF16):
        peak: ((($max_sclk * $cu_per_gpu) * 4096) / 1000)
        pop: 100 * SUM(SQ_INSTS_VALU_MFMA_MOPS_BF16 * 512) / SUM(End_Timestamp - Start_Timestamp) / ($max_sclk * $cu_per_gpu * 4096 / 1000)
    - MFMA FLOPs (F16):
        peak: ((($max_sclk * $cu_per_gpu) * 4096) / 1000)
        pop: 100 * SUM(SQ_INSTS_VALU_MFMA_MOPS_F16 * 512) / SUM(End_Timestamp - Start_Timestamp) / ($max_sclk * $cu_per_gpu * 4096 / 1000)
    - MFMA FLOPs (F64):
        peak: ((($max_sclk * $cu_per_gpu) * 128) / 1000)
        pop: 100 * SUM(SQ_INSTS_VALU_MFMA_MOPS_F64 * 512) / SUM(End_Timestamp - Start_Timestamp) / ($max_sclk * $cu_per_gpu * 128 / 1000)
    - MFMA IOPs (Int8):
        peak: ((($max_sclk * $cu_per_gpu) * 8192) / 1000)
        pop: 100 * SUM(SQ_INSTS_VALU_MFMA_MOPS_I8 * 512) / SUM(End_Timestamp - Start_Timestamp) / ($max_sclk * $cu_per_gpu * 8192 / 1000)
    - L2-Fabric Read BW:
        value: SUM(128 * TCC_EA0_RDREQ_128B_sum + 64 * TCC_EA0_RDREQ_64B_sum + 32 * TCC_EA0_RDREQ_32B_sum) / SUM(End_Timestamp - Start_Timestamp)
        pop: 100 * (SUM(128 * TCC_EA0_RDREQ_128B_sum + 64 * TCC_EA0_RDREQ_64B_sum + 32 * TCC_EA0_RDREQ_32B_sum) / SUM(End_Timestamp - Start_Timestamp)) / $hbmBandwidth
- Panel Config:
    id: 200
  metric_descriptions:
    Theoretical LDS Bandwidth: Indicates the maximum amount of bytes that could have been loaded from, stored to, or atomically updated in the LDS per unit time (see LDS Bandwidth example for more detail).
    L1I Hit Rate: The percent of L1I requests that hit on a previously loaded line the cache. Calculated as the ratio of the number of L1I requests that hit over the number of all L1I requests.
    L1I BW: The number of bytes looked up in the L1I cache per unit time. This is also presented as a percent of the peak theoretical bandwidth achievable on the specific accelerator.
- Panel Config:
    id: 300
  metric_table:
    id: 301
    metrics:
    - Wavefronts:
        value: ROUND(AVG(SPI_CS0_WAVE + SPI_CS1_WAVE + SPI_CS2_WAVE + SPI_CS3_WAVE), 0)
    - Workgroups:
        value: ROUND(AVG(SPI_CS0_NUM_THREADGROUPS + SPI_CS1_NUM_THREADGROUPS + SPI_CS2_NUM_THREADGROUPS + SPI_CS3_NUM_THREADGROUPS), 0)
- Panel Config:
    id: 400
  metric_table:
    id: 401
    metrics:
    - HBM Bandwidth:
        value: SUM(TCC_EA0_RDREQ_128B_sum * 128 + TCC_EA0_RDREQ_32B_sum * 32 + TCC_EA0_RDREQ_64B_sum * 64 + (TCC_EA0_WRREQ_sum - TCC_EA0_WRREQ_64B_sum) * 32 + TCC_EA0_WRREQ_64B_sum * 64) / SUM(End_Timestamp - Start_Timestamp)
- Panel Config:
    id: 400
  metric_table:
    id: 402
    metrics:
    - AI HBM:
        value: ( SUM( ($wave_size * ( (SQ_INSTS_VALU_ADD_F16 + SQ_INSTS_VALU_MUL_F16 + (2 * SQ_INSTS_VALU_FMA_F16) + SQ_INSTS_VALU_TRANS_F16) + (SQ_INSTS_VALU_ADD_F32 + SQ_INSTS_VALU_MUL_F32 + (2 * SQ_INSTS_VALU_FMA_F32) + SQ_INSTS_VALU_TRANS_F32) + (SQ_INSTS_VALU_ADD_F64 + SQ_INSTS_VALU_MUL_F64 + (2 * SQ_INSTS_VALU_FMA_F64) + SQ_INSTS_VALU_TRANS_F64) )) + (SQ_INSTS_VALU_MFMA_MOPS_F16 * 512) + (SQ_INSTS_VALU_MFMA_MOPS_BF16 * 512) + (SQ_INSTS_VALU_MFMA_MOPS_F32 * 512) + (SQ_INSTS_VALU_MFMA_MOPS_F64 * 512) + (SQ_INSTS_VALU_MFMA_MOPS_F8 * 512) + (SQ_INSTS_VALU_MFMA_MOPS_F6F4 * 512) ) / SUM( (TCC_EA0_RDREQ_128B_sum * 128) + (TCC_EA0_RDREQ_32B_sum * 32) + (TCC_EA0_RDREQ_64B_sum * 64) + ((TCC_EA0_WRREQ_sum - TCC_EA0_WRREQ_64B_sum) * 32) + (TCC_EA0_WRREQ_64B_sum * 64) ) )
    - AI L2:
        value: ( SUM( ($wave_size * ( (SQ_INSTS_VALU_ADD_F16 + SQ_INSTS_VALU_MUL_F16 + (2 * SQ_INSTS_VALU_FMA_F16) + SQ_INSTS_VALU_TRANS_F16) + (SQ_INSTS_VALU_ADD_F32 + SQ_INSTS_VALU_MUL_F32 + (2 * SQ_INSTS_VALU_FMA_F32) + SQ_INSTS_VALU_TRANS_F32) + (SQ_INSTS_VALU_ADD_F64 + SQ_INSTS_VALU_MUL_F64 + (2 * SQ_INSTS_VALU_FMA_F64) + SQ_INSTS_VALU_TRANS_F64) )) + (SQ_INSTS_VALU_MFMA_MOPS_F16 * 512) + (SQ_INSTS_VALU_MFMA_MOPS_BF16 * 512) + (SQ_INSTS_VALU_MFMA_MOPS_F32 * 512) + (SQ_INSTS_VALU_MFMA_MOPS_F64 * 512) + (SQ_INSTS_VALU_MFMA_MOPS_F8 * 512) + (SQ_INSTS_VALU_MFMA_MOPS_F6F4 * 512) ) / SUM( (TCP_TCC_WRITE_REQ_sum + TCP_TCC_ATOMIC_WITH_RET_REQ_sum + TCP_TCC_ATOMIC_WITHOUT_RET_REQ_sum + TCP_TCC_READ_REQ_sum) * 64 ) )
    - AI L1:
        value: ( SUM( ($wave_size * ( (SQ_INSTS_VALU_ADD_F16 + SQ_INSTS_VALU_MUL_F16 + (2 * SQ_INSTS_VALU_FMA_F16) + SQ_INSTS_VALU_TRANS_F16) + (SQ_INSTS_VALU_ADD_F32 + SQ_INSTS_VALU_MUL_F32 + (2 * SQ_INSTS_VALU_FMA_F32) + SQ_INSTS_VALU_TRANS_F32) + (SQ_INSTS_VALU_ADD_F64 + SQ_INSTS_VALU_MUL_F64 + (2 * SQ_INSTS_VALU_FMA_F64) + SQ_INSTS_VALU_TRANS_F64) )) + (SQ_INSTS_VALU_MFMA_MOPS_F16 * 512) + (SQ_INSTS_VALU_MFMA_MOPS_BF16 * 512) + (SQ_INSTS_VALU_MFMA_MOPS_F32 * 512) + (SQ_INSTS_VALU_MFMA_MOPS_F64 * 512) + (SQ_INSTS_VALU_MFMA_MOPS_F8 * 512) + (SQ_INSTS_VALU_MFMA_MOPS_F6F4 * 512) ) / SUM(TCP_TOTAL_CACHE_ACCESSES_sum * 64) )
    - Performance (GFLOPs):
        value: ( SUM( ($wave_size * ( (SQ_INSTS_VALU_ADD_F16 + SQ_INSTS_VALU_MUL_F16 + (2 * SQ_INSTS_VALU_FMA_F16) + SQ_INSTS_VALU_TRANS_F16) + (SQ_INSTS_VALU_ADD_F32 + SQ_INSTS_VALU_MUL_F32 + (2 * SQ_INSTS_VALU_FMA_F32) + SQ_INSTS_VALU_TRANS_F32) + (SQ_INSTS_VALU_ADD_F64 + SQ_INSTS_VALU_MUL_F64 + (2 * SQ_INSTS_VALU_FMA_F64) + SQ_INSTS_VALU_TRANS_F64) )) + (SQ_INSTS_VALU_MFMA_MOPS_F16 * 512) + (SQ_INSTS_VALU_MFMA_MOPS_BF16 * 512) + (SQ_INSTS_VALU_MFMA_MOPS_F32 * 512) + (SQ_INSTS_VALU_MFMA_MOPS_F64 * 512) + (SQ_INSTS_VALU_MFMA_MOPS_F8 * 512) + (SQ_INSTS_VALU_MFMA_MOPS_F6F4 * 512) ) / (SUM(End_Timestamp - Start_Timestamp) / 1e9) ) / 1e9
- Panel Config:
    id: 600
  metric_table:
    id: 601
    metrics:
    - Scheduler-Pipe Utilization:
        avg: SUM(100 * (SPI_CS0_BUSY + SPI_CS1_BUSY + SPI_CS2_BUSY + SPI_CS3_BUSY)) / SUM($GRBM_GUI_ACTIVE_PER_XCD * $pipes_per_gpu * $se_per_gpu)
        min: MIN(100 * (SPI_CS0_BUSY + SPI_CS1_BUSY + SPI_CS2_BUSY + SPI_CS3_BUSY) / ($GRBM_GUI_ACTIVE_PER_XCD * $pipes_per_gpu * $se_per_gpu))
        max: MAX(100 * (SPI_CS0_BUSY + SPI_CS1_BUSY + SPI_CS2_BUSY + SPI_CS3_BUSY) / ($GRBM_GUI_ACTIVE_PER_XCD * $pipes_per_gpu * $se_per_gpu))
    - Dispatched Workgroups:
        avg: AVG(SPI_CS0_NUM_THREADGROUPS + SPI_CS1_NUM_THREADGROUPS + SPI_CS2_NUM_THREADGROUPS + SPI_CS3_NUM_THREADGROUPS)
        min: MIN(SPI_CS0_NUM_THREADGROUPS + SPI_CS1_NUM_THREADGROUPS + SPI_CS2_NUM_THREADGROUPS + SPI_CS3_NUM_THREADGROUPS)
        max: MAX(SPI_CS0_NUM_THREADGROUPS + SPI_CS1_NUM_THREADGROUPS + SPI_CS2_NUM_THREADGROUPS + SPI_CS3_NUM_THREADGROUPS)
    - Dispatched Wavefronts:
        avg: AVG(SPI_CS0_WAVE + SPI_CS1_WAVE + SPI_CS2_WAVE + SPI_CS3_WAVE)
        min: MIN(SPI_CS0_WAVE + SPI_CS1_WAVE + SPI_CS2_WAVE + SPI_CS3_WAVE)
        max: MAX(SPI_CS0_WAVE + SPI_CS1_WAVE + SPI_CS2_WAVE + SPI_CS3_WAVE)
    - VGPR Writes:
        avg: SUM(SPI_VWC0_VDATA_VALID_WR + SPI_VWC1_VDATA_VALID_WR) / SUM(SPI_CS0_WAVE + SPI_CS1_WAVE + SPI_CS2_WAVE + SPI_CS3_WAVE)
        min: MIN((SPI_VWC0_VDATA_VALID_WR + SPI_VWC1_VDATA_VALID_WR) / (SPI_CS0_WAVE + SPI_CS1_WAVE + SPI_CS2_WAVE + SPI_CS3_WAVE))
        max: MAX((SPI_VWC0_VDATA_VALID_WR + SPI_VWC1_VDATA_VALID_WR) / (SPI_CS0_WAVE + SPI_CS1_WAVE + SPI_CS2_WAVE + SPI_CS3_WAVE))
    - SGPR Writes:
        avg: SUM(1 * SPI_SWC_CSC_WR) / SUM(SPI_CS0_WAVE + SPI_CS1_WAVE + SPI_CS2_WAVE + SPI_CS3_WAVE)
        min: MIN(1 * SPI_SWC_CSC_WR / (SPI_CS0_WAVE + SPI_CS1_WAVE + SPI_CS2_WAVE + SPI_CS3_WAVE))
        max: MAX(1 * SPI_SWC_CSC_WR / (SPI_CS0_WAVE + SPI_CS1_WAVE + SPI_CS2_WAVE + SPI_CS3_WAVE))
- Panel Config:
    id: 700
  metric_table:
    id: 701
    metrics:
    - Total Wavefronts:
        avg: AVG(SPI_CS0_WAVE + SPI_CS1_WAVE + SPI_CS2_WAVE + SPI_CS3_WAVE)
        min: MIN(SPI_CS0_WAVE + SPI_CS1_WAVE + SPI_CS2_WAVE + SPI_CS3_WAVE)
        max: MAX(SPI_CS0_WAVE + SPI_CS1_WAVE + SPI_CS2_WAVE + SPI_CS3_WAVE)
- Panel Config:
    id: 1100
  metric_table:
    id: 1101
    metrics:
    - MFMA FLOPs (F8):
        peak: ((($max_sclk * $cu_per_gpu) * 8192) / 1000)
        pop: 100 * SUM(SQ_INSTS_VALU_MFMA_MOPS_F8 * 512) / SUM(End_Timestamp - Start_Timestamp) / ($max_sclk * $cu_per_gpu * 8192 / 1000)
    - MFMA FLOPs (BF16):
        peak: ((($max_sclk * $cu_per_gpu) * 4096) / 1000)
        pop: 100 * SUM(SQ_INSTS_VALU_MFMA_MOPS_BF16 * 512) / SUM(End_Timestamp - Start_Timestamp) / ($max_sclk * $cu_per_gpu * 4096 / 1000)
    - MFMA FLOPs (F16):
        peak: ((($max_sclk * $cu_per_gpu) * 4096) / 1000)
        pop: 100 * SUM(SQ_INSTS_VALU_MFMA_MOPS_F16 * 512) / SUM(End_Timestamp - Start_Timestamp) / ($max_sclk * $cu_per_gpu * 4096 / 1000)
    - MFMA FLOPs (F64):
        peak: ((($max_sclk * $cu_per_gpu) * 128) / 1000)
        pop: 100 * SUM(SQ_INSTS_VALU_MFMA_MOPS_F64 * 512) / SUM(End_Timestamp - Start_Timestamp) / ($max_sclk * $cu_per_gpu * 128 / 1000)
    - MFMA IOPs (INT8):
        peak: ((($max_sclk * $cu_per_gpu) * 8192) / 1000)
        pop: 100 * SUM(SQ_INSTS_VALU_MFMA_MOPS_I8 * 512) / SUM(End_Timestamp - Start_Timestamp) / ($max_sclk * $cu_per_gpu * 8192 / 1000)
- Panel Config:
    id: 1100
  metric_table:
    id: 1103
    metrics:
    - FLOPs (Total):
        avg: SUM(((((64 * (SQ_INSTS_VALU_ADD_F16 + SQ_INSTS_VALU_MUL_F16 + SQ_INSTS_VALU_TRANS_F16 + SQ_INSTS_VALU_FMA_F16 * 2) + (512 * SQ_INSTS_VALU_MFMA_MOPS_F8 + 512 * SQ_INSTS_VALU_MFMA_MOPS_F16 + 512 * SQ_INSTS_VALU_MFMA_MOPS_BF16)) + 64 * (SQ_INSTS_VALU_ADD_F32 + SQ_INSTS_VALU_MUL_F32 + SQ_INSTS_VALU_TRANS_F32 + SQ_INSTS_VALU_FMA_F32 * 2)) + 512 * SQ_INSTS_VALU_MFMA_MOPS_F32) + 64 * (SQ_INSTS_VALU_ADD_F64 + SQ_INSTS_VALU_MUL_F64 + SQ_INSTS_VALU_TRANS_F64 + SQ_INSTS_VALU_FMA_F64 * 2)) + 512 * SQ_INSTS_VALU_MFMA_MOPS_F64 + 512 * SQ_INSTS_VALU_MFMA_MOPS_F6F4) / SUM($denom)
        min: MIN((((((((64 * (((SQ_INSTS_VALU_ADD_F16 + SQ_INSTS_VALU_MUL_F16) + SQ_INSTS_VALU_TRANS_F16) + (SQ_INSTS_VALU_FMA_F16 * 2))) + ((512 * SQ_INSTS_VALU_MFMA_MOPS_F8) + (512 * SQ_INSTS_VALU_MFMA_MOPS_F16) + (512 * SQ_INSTS_VALU_MFMA_MOPS_BF16))) + (64 * (((SQ_INSTS_VALU_ADD_F32 + SQ_INSTS_VALU_MUL_F32) + SQ_INSTS_VALU_TRANS_F32) + (SQ_INSTS_VALU_FMA_F32 * 2)))) + (512 * SQ_INSTS_VALU_MFMA_MOPS_F32)) + (64 * (((SQ_INSTS_VALU_ADD_F64 + SQ_INSTS_VALU_MUL_F64) + SQ_INSTS_VALU_TRANS_F64) + (SQ_INSTS_VALU_FMA_F64 * 2)))) + (512 * SQ_INSTS_VALU_MFMA_MOPS_F64) + (512 * SQ_INSTS_VALU_MFMA_MOPS_F6F4)) / $denom))
        max: MAX((((((((64 * (((SQ_INSTS_VALU_ADD_F16 + SQ_INSTS_VALU_MUL_F16) + SQ_INSTS_VALU_TRANS_F16) + (SQ_INSTS_VALU_FMA_F16 * 2))) + ((512 * SQ_INSTS_VALU_MFMA_MOPS_F8) + (512 * SQ_INSTS_VALU_MFMA_MOPS_F16) + (512 * SQ_INSTS_VALU_MFMA_MOPS_BF16))) + (64 * (((SQ_INSTS_VALU_ADD_F32 + SQ_INSTS_VALU_MUL_F32) + SQ_INSTS_VALU_TRANS_F32) + (SQ_INSTS_VALU_FMA_F32 * 2)))) + (512 * SQ_INSTS_VALU_MFMA_MOPS_F32)) + (64 * (((SQ_INSTS_VALU_ADD_F64 + SQ_INSTS_VALU_MUL_F64) + SQ_INSTS_VALU_TRANS_F64) + (SQ_INSTS_VALU_FMA_F64 * 2)))) + (512 * SQ_INSTS_VALU_MFMA_MOPS_F64) + (512 * SQ_INSTS_VALU_MFMA_MOPS_F6F4)) / $denom))
    - IOPs (Total):
        unit: (IOPS + $normUnit)
    - INT8 OPs:
        unit: (IOPS + $normUnit)
- Panel Config:
    id: 1400
  metric_descriptions:
    Atomic Req: The total number of atomic requests from sL1D to the L2, per normalization unit. Atomic requests to the sL1D are typically unused on current CDNA accelerators.
- Panel Config:
    id: 1700
  metric_table:
    id: 1701
    metrics:
    - L2-Fabric Read BW:
        value: SUM(TCC_EA0_RDREQ_32B_sum * 32 + TCC_EA0_RDREQ_64B_sum * 64 + TCC_EA0_RDREQ_128B_sum * 128) / SUM(End_Timestamp - Start_Timestamp)
- Panel Config:
    id: 1700
  metric_table:
    id: 1702
    metrics:
    - Read BW:
        avg: SUM(TCC_EA0_RDREQ_32B_sum * 32 + TCC_EA0_RDREQ_64B_sum * 64 + TCC_EA0_RDREQ_128B_sum * 128) / SUM(End_Timestamp - Start_Timestamp)
        min: MIN((((TCC_EA0_RDREQ_32B_sum * 32) + (TCC_EA0_RDREQ_64B_sum * 64) + (TCC_EA0_RDREQ_128B_sum * 128)) / (End_Timestamp - Start_Timestamp)))
        max: MAX((((TCC_EA0_RDREQ_32B_sum * 32) + (TCC_EA0_RDREQ_64B_sum * 64) + (TCC_EA0_RDREQ_128B_sum * 128)) / (End_Timestamp - Start_Timestamp)))
- Panel Config:
    id: 1700
  metric_table:
    id: 1706
    metrics:
    - Read (64B):
        avg: SUM(TCC_EA0_RDREQ_64B_sum) / SUM($denom)
        min: MIN((TCC_EA0_RDREQ_64B_sum / $denom))
        max: MAX((TCC_EA0_RDREQ_64B_sum / $denom))
    - HBM Write and Atomic:
        avg: SUM(TCC_EA0_WRREQ_WRITE_DRAM_sum) / SUM($denom)
        min: MIN((TCC_EA0_WRREQ_WRITE_DRAM_sum / $denom))
        max: MAX((TCC_EA0_WRREQ_WRITE_DRAM_sum / $denom))
- Panel Config:
    id: 1800
  metric_table:
    id: 1809
    metrics:
    - ::_1:
        ea read stall - pcie: SUM(TO_INT(TCC_EA0_RDREQ_IO_CREDIT_STALL[::_1])) / SUM($denom)
        ea read stall - if: SUM(TO_INT(TCC_EA0_RDREQ_GMI_CREDIT_STALL[::_1])) / SUM($denom)
        ea read stall - hbm: SUM(TO_INT(TCC_EA0_RDREQ_DRAM_CREDIT_STALL[::_1])) / SUM($denom)
- Panel Config:
    id: 1800
  metric_table:
    id: 1810
    metrics:
    - ::_1:
        ea write stall - pcie: SUM(TO_INT(TCC_EA0_WRREQ_IO_CREDIT_STALL[::_1])) / SUM($denom)
        ea write stall - if: SUM(TO_INT(TCC_EA0_WRREQ_GMI_CREDIT_STALL[::_1])) / SUM($denom)
        ea write stall - hbm: SUM(TO_INT(TCC_EA0_WRREQ_DRAM_CREDIT_STALL[::_1])) / SUM($denom)
